import React from 'react';
import { GlassPanel } from '../../ui/common/GlassPanel';

export function FounderStory() {
  return (
    <GlassPanel>
      <div>
        {/* Intentionally minimal placeholder to restore TypeScript validity. */}
      </div>
    </GlassPanel>
  );
}

